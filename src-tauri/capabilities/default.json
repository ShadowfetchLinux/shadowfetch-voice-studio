{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the main window. No shell, fs or http plugins: every file the UI touches goes through a typed Rust command that re-validates the path.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:path:default",
    "core:window:default",
    "dialog:default",
    "opener:default"
  ]
}
